# profile

Owner's display name, bio, and avatar. Same layered structure as
`social-links` (`repository.ts`, `service.ts`, `actions.ts`, `schema.ts`),
own dashboard section. See [../README.md](../README.md) for cross-module
import rules.
